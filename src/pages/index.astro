---
import Layout from '@layouts/Layout.astro';
import { Hero, type Locale } from '@widgets/components';

const { currentLocale } = Astro;
---

<Layout>
  <Hero locale={currentLocale as Locale} client:load />
</Layout>
