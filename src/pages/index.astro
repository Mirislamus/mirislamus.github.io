---
import type { Locale } from '@typings/global';
import Layout from '@layouts/Layout.astro';
import { Approach, Hero, Skills, Career, Projects, Reviews } from '@widgets/components';

const locale = Astro.currentLocale as Locale;
---

<Layout>
  <Hero locale={locale} client:load />
  <Approach locale={locale} client:visible />
  <Skills locale={locale} client:visible />
  <Career locale={locale} client:visible />
  <Projects locale={locale} client:visible />
  <Reviews locale={locale} client:visible />
</Layout>
